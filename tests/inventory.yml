hosts:
  ubuntu-1:
    user: vagrant # Optional
    port: 22 # Optional
    deploy_dir: ~/hapideploy/{{stage}} # Optional
  ubuntu-2:
    deploy_dir: ~/hapideploy/{{stage}}
  ubuntu-3:
    deploy_dir: ~/hapideploy/{{stage}}
